/**
 * Site-wide toggles (edit and commit — no console or localStorage).
 *
 * Scroll debug: fixed overlay with lenis Y / native window Y / max scroll (px), useful when
 * tuning section heights or TOC behavior. Set to false so visitors don’t see it on GitHub Pages.
 */
export const SCROLL_DEBUG_OVERLAY = true;
